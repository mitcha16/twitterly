<% provide(:title, "Home") %>

<div class="container">
  <br>
  <% if !logged_in? %>
  <div class="well">
    <div class="row">
      <div class="col-md-8">
        <h4>Receive free coupons, free shipping, and a T-Shirt!*</h4>
      </div>
      <div class="col-md-4">
        <%= link_to "Sign Up", users_new_path,
        class: "btn btn-lg btn-success btn-block"%>
      </div>
    </div>
  </div>
  <% end %>
  <div class="row">
    <%= render "partials/carousel" %>
  </div>
  <h5>**Not Really**</h5>
</div>
